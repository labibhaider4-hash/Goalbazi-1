# Root copy for Railway when the service builds from the repository root.
# The actual app files live in goalbazi/, but Nixpacks looks here first.
flask==3.0.3
psycopg2-binary==2.9.9
gunicorn==22.0.0
pywebpush==2.0.3
